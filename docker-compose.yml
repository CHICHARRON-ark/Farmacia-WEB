services:
  glowbeauty-nginx:
    image: nginx:1.27-alpine
    container_name: glowbeauty-local-nginx
    ports:
      - "8080:80"
    volumes:
      - ./deploy/local/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/local/www:/var/www/html:ro
    restart: unless-stopped
